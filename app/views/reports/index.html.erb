<section class="report_index">
  <h1>投稿一覧</h1>
    <div><%= link_to "新規投稿", new_report_path, class: "btn" %></div>
      <% @reports.each do |report| %>
        <section class="report">
          <% if report.user.image? %>
            <%= image_tag report.user.image.thumb50.url, class: 'user-image' %>
          <% else %>
            <%= image_tag 'noimage.png', :size => '50x50', class: 'user-image' %>
          <% end %>
          <%= link_to report.user.username, user_path(report.user_id) %>
          <p><%= link_to report.title, report_path(report.id) %></p>
          <p><%= image_tag report.image.url, class: 'image' %></p>
          <p><%= render 'favorites/fav_button', report: @report %></p>
          <%= "#{report.user.username}: #{report.content}" %>
          <p><%= link_to "コメントする", report_path(report.id) %></p>
          <%= report.created_at %>
        </section>
      <% end %>
    <%= paginate @reports %>
</section>